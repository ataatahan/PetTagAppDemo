@model IList<PetTag.Service.DTOs.PetOwnerListItemDto>
@{
    ViewData["Title"] = "Pet Sahipleri";
}

<div class="page-header d-flex justify-content-between align-items-center">
    <div>
        <h1><i class="bi bi-people"></i> Pet Sahipleri</h1>
        <p class="text-muted mb-0">Tüm pet sahiplerini görüntüleyin ve yönetin</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-person-plus"></i> Yeni Pet Sahibi Ekle
    </a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle"></i> @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle"></i> @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-body">
        <form method="get" asp-action="Index" class="mb-3">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="İsim, soyisim veya email ile ara..." value="@ViewContext.HttpContext.Request.Query["search"]">
                <button class="btn btn-outline-secondary" type="submit">
                    <i class="bi bi-search"></i> Ara
                </button>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>İsim</th>
                        <th>Email</th>
                        <th class="text-end">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var owner in Model)
                        {
                            <tr>
                                <td>@owner.Id</td>
                                <td><strong>@owner.FullName</strong></td>
                                <td>@owner.Email</td>
                                <td class="text-end">
                                    <div class="action-buttons">
                                        <a asp-action="Details" asp-route-id="@owner.Id" class="btn btn-sm btn-info" title="Detay">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@owner.Id" class="btn btn-sm btn-warning" title="Düzenle">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <form method="post" asp-action="SoftDelete" asp-route-id="@owner.Id" style="display:inline;">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-secondary" title="Pasif Yap" onclick="return confirm('Bu pet sahibini pasif yapmak istediğinizden emin misiniz?');">
                                                <i class="bi bi-pause-circle"></i>
                                            </button>
                                        </form>
                                        <form method="post" asp-action="Delete" asp-route-id="@owner.Id" style="display:inline;">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Sil" onclick="return confirm('Bu pet sahibini kalıcı olarak silmek istediğinizden emin misiniz?');">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="text-center py-5">
                                <div class="empty-state">
                                    <i class="bi bi-inbox"></i>
                                    <p class="mt-3">Henüz pet sahibi kaydı bulunmamaktadır.</p>
                                    <a asp-action="Create" class="btn btn-primary">
                                        <i class="bi bi-person-plus"></i> İlk Pet Sahibini Ekle
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

